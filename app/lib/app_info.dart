// Single source for the version string shown on the App info screen.
// Keep in sync with pubspec.yaml's `version:` -- there's no package_info_plus
// dependency yet to read it at runtime, and adding one for a single label
// isn't worth it.
const kAppVersion = '1.0.0';
const kAppBuild = '1';
